<div class="container">
	<h1>Editing Employee</h1>
	
	<% if logged_in? %>	
		<%= render 'form', employee: @employee %>
	<% else %>
	<h3>You need the right authentication to view this page</h3>
<% end %>

	<%= link_to 'Show', @employee %> |
	<%= link_to 'Back', employee_path %>
</div>
